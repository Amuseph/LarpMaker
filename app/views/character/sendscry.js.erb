$('#sendCourierModalBody').html("<%= escape_javascript( render partial: 'modals/sendscry' ) %>")
$('#sendCourierModalLabel').html("Send Scry")
$('#sendCourierModal').modal({
    backdrop: 'static',
    keyboard: false,
    show: true});