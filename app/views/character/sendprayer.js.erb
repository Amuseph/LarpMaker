$('#sendCourierModalBody').html("<%= escape_javascript( render partial: 'modals/sendprayer' ) %>")
$('#sendCourierModalLabel').html("Send New Prayer")
$('#sendCourierModal').modal({
    backdrop: 'static',
    keyboard: false,
    show: true});